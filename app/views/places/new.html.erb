<h1><%= @title %> </h1>

<% @places.each do |place| %>
  <div class='new_place_show'>
    <h2><%= link_to place.name, place_path(place) %></h2>
  </div>
<% end %>

<%= render 'form' %>

<%= link_to 'Back', places_path %>
